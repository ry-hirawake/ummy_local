# Project Tech Rules

## Status
Draft | Accepted（←どちらか）

## 1) このプロジェクトで「絶対に守る」一言
- 方針：<プロジェクトの技術方針を1文で>

---

## 2) 採用（Tech Stack）
- 言語：< >
- UI：< >（UIがある場合）
- 通信/外部I/F：< >（API/DB/Queue など）
- 非同期/並行処理：< >
- テスト：< >
- 最低サポート環境（任意）：<OS/Runtime/Version>
- 外部ライブラリ方針：<最小限/許可/禁止>

---

## 3) 禁止（Non-negotiable）
> 「使用禁止」だけでなく「提案禁止」も明記する

- 禁止（使用）：< >
- 禁止（提案も禁止）：< >
- 禁止（その他）：< >

---

## 4) 境界（Boundaries：どこで何を分けるか）
> “漏れると設計が崩れる境界” だけ書く（細かすぎるルールは後で）

- データ表現の境界：<例：外部I/F用モデル ↔ 内部モデル は分離し、境界で変換>
- UI/Domain/Data の依存：<例：DomainはUI/Frameworkに依存しない>
- 層/モジュールの責務：<簡潔に>
- 依存の禁止：<例：UIが外部I/Fモデルに依存しない>

---

## 5) エラー・例外・非同期（最低限）
- Null/未定義の扱い：<例：未定義は早期return、強制アンラップ禁止>
- 例外/エラー方針：<例：握り潰し禁止、境界でエラーを型として返す>
- 非同期：<例：キャンセル尊重、タイムアウト方針>
- ログ：<例：PII禁止、レベル方針、出力先>

---

## 6) 外部I/F（API/DB/Queue 等の最低ルール）
- I/Fモデル：<例：外部I/Fモデルと内部モデルは分離>
- 失敗時の扱い：<例：リトライ方針、エラー変換>
- 観測性：<例：メトリクス/トレース/ログ>

---

## 7) テスト（最低ルール）
- 何をテストする：<重要な振る舞い/境界/回帰ポイント>
- 実行方法：<コマンド>
- 速度方針：<例：ユニット優先、重いテストは別ジョブ>

---

## 8) 品質ゲート（マージ/リリースの最低条件）
- 必須：<例：ビルド、静的解析、テスト>
- 禁止検出（あれば）：<例：特定依存/特定APIの使用を検出したら失敗>

---

## 9) 例外手続き（破る場合の手順）
- ルール例外が必要になったら：  
  - [ ] ADRを作る（`.trixa/03_design/adrs/adr-XXXX.md`）  
  - [ ] 影響範囲（Spec/Tests）を更新  
  - [ ] CHANGELOGに1行残す（`.trixa/99_changelog/CHANGELOG.md`）  
  - [ ] このファイルも更新する

---

## 10) 変更履歴（このファイル内）
- YYYY-MM-DD：<変更> / reason:<理由> / impact:<影響>